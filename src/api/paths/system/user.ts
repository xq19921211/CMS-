const base = '/user';
export default {
    base: `${base}`,
    list: `${base}/list`,
    password: `${base}/password`,
    role: `${base}/role`,
    register: `${base}/register`,
    info: `${base}/info`
};

